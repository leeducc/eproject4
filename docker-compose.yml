version: '3.8'

services:
  nginx:
    image: nginx:latest
    container_name: englishhub-nginx
    ports:
      - "80:80"
    volumes:
      
      - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf:ro
      
      - ./backend/uploads:/var/www/uploads:ro 
    restart: unless-stopped
