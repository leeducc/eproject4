# API Documentation Maintenance Rule

Whenever a new backend API endpoint is created or an existing one is modified (e.g., in a `@RestController` class), the AI assistant MUST update the `document/api_endpoints.md` file to reflect the changes.

The update should include:

- Endpoint path
- HTTP Method
- Description
- Request JSON payload (if any)
- Response JSON payload example
